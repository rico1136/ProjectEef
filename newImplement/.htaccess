RewriteEngine On
RewriteBase /newImplement/

# Allow direct access to existing files (like images, CSS, etc.)
RewriteCond %{REQUEST_FILENAME} !-f

# Route all requests to index.php
RewriteRule ^(.*)$ index.php [QSA,L]
